const express = require("express");
const router = express.Router();
const validUrl = require("valid-url")
const validUrl = require("shortid");
const validUrl = require("config");


const Url = require("../../models/url")
module.exports = router;